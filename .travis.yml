language: node_js
node_js:
  - '0.12'
install:
  - npm install
  - bundle install
script:
  - grunt build
after_success:
  - test $TRAVIS_PULL_REQUEST == "false" && test $TRAVIS_BRANCH == "master" && grunt gh-pages:travis
