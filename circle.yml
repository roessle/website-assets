machine:
  node:
    version: 0.12
    
test:  
  override:
    - grunt build

deployment:
  production:
    branch: master
    commands:
      - git config --global user.email "circleci@roessle.eu"
      - git config --global user.name "Roessle CircleCI"
      - grunt gh-pages:ci
